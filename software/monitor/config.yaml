# EMP-Guardian - Monitoring application configuration
# Author: Ciprian Ștefan Pleșca

log_file: "emp_events.log"

# Thresholds and notification options (extensible - implement the
# corresponding notification functions in emp_monitor.py or
# software/api/ according to your institution's needs)
notifications:
  enabled: false
  # webhook_url: "https://example.local/webhook"
  # email_to: "operator@example.local"

# Automatic serial-port reconnection interval, in seconds
reconnect_interval_s: 5
